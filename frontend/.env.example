# Backend API URL (Flask server)
# For local development, leave empty to use Vite proxy (defaults to http://localhost:5000)
# For production (Vercel), set to your deployed Flask backend URL
# Example: VITE_API_URL=https://your-backend.onrender.com
VITE_API_URL=
