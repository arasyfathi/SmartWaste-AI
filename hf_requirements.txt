# ─────────────────────────────────────────────────────────────────────────────
# hf_requirements.txt — Hugging Face Spaces deployment (CPU-only, Docker)
#
# Optimized for HF Spaces free tier (no GPU, limited RAM/disk).
# Uses CPU-only PyTorch to keep image size manageable.
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0

# Core
numpy>=1.24.0,<2.0.0
pillow>=10.0.0
opencv-python-headless>=4.8.0

# AI — Classification (TensorFlow CPU)
tensorflow-cpu>=2.16.0

# AI — Detection (PyTorch CPU + YOLOv8)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# Google Drive (optional — only if token.json is provided)
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0
